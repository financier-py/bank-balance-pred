[project]
name = "bank-ts-pred"
version = "0.1.0"
description = "Prototype for multi-horizon bank deposit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=3.0.3",
]
